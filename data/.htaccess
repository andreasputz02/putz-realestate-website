# Block all direct access to the files in this folder — they contain
# personal data and must only ever be read server-side by our own PHP scripts.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
