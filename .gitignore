.DS_Store
.claude/

# Personenbezogene Daten — Namen, E-Mail-Adressen, IBANs.
# Duerfen niemals ins Repository. data/.htaccess bleibt versioniert,
# denn die Datei ist es, die den Ordner nach aussen abschottet.
data/*.json
data/*.sqlite
data/*.sqlite-wal
data/*.sqlite-shm

# Justimmo-Zugangsdaten — duerfen niemals ins Repository
justimmo-config.php

Haus.jpg
Video Siegenfeld.MOV
Video Brunn.MOV
Wohnung-Wallrissstraße.MP4

# Originaldateien der Verkaufsseite — die verkleinerten Fassungen liegen
# unter assets/img/schritte/ und assets/img/fotografie/. Die Originale
# bleiben lokal liegen, gehoeren aber nicht ins Web-Verzeichnis.
/kennenlernen.png
/vorbereitung.png
/Verfilmung.png
/Vermarktung.png
/abschluss.png
/übergabe.png
/fotos-verkaufenseite/

# Originalfotos der verkauften Objekte — die verkleinerten
# Fassungen liegen unter assets/img/verkauft/.
/Verkaufte Objekte/

# Objekt Wallrissstrasse — von der Website genommen. Die Dateien
# bleiben lokal liegen, falls sie noch einmal gebraucht werden.
/assets/img/objekte/wallriss/
/assets/video/immobilie-wallrissstrasse.mp4
/assets/img/immobilie-wallriss-poster.jpg

# Lokaler Vorschauserver, gehoert nicht auf den Webspace.
/.dev-server.py

# Rohvideos — die webtauglichen Fassungen liegen unter assets/video/.
*.MOV
*.mov
